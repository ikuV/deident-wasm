# build artifacts
/target
**/*.wasm

# agent working files
AGENT_PLAN*.md

# per-job workspaces & local run outputs
/jobs
out.csv
report.json
examples/chains/out/
*.vault
*.vault.jsonl

# DICOM instances. Never commit these: they are large and routinely carry
# patient identifiers in their metadata (and possibly burned into the pixels).
# Test fixtures are generated instead — see crates/dicom/examples/gen_fixtures.rs
*.dcm
*.DCM
*.dicom
DICOMDIR

# secrets / local env
.env
.env.*

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp
